@model IEnumerable<PizzaStoreMvc.Client.DomainModels.PizzaCheese>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cheese.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pizza.PizzaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cheese.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pizza.PizzaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PizzaCheeseID }) 
            @*@Html.ActionLink("Details", "Details", new { id=item.PizzaCheeseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PizzaCheeseID })*@
        </td>
    </tr>
}

</table>
